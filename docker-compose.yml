services:
  postgres:
    image: postgres:17

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: kingkuttu
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: taskflow

    volumes: 
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

  